#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-linter/bin/grunt-linter" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-linter/bin/grunt-linter" "$@"
  ret=$?
fi
exit $ret
